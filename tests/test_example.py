"""Basic example test module.

This exists so that `invoke test` / `pytest` has at least one passing test
and to demonstrate where tests should live in this template.
"""


def test_example_always_passes() -> None:
    """A trivial test that always passes."""
    assert True is True
